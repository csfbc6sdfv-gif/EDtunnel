{
  "name": "song0810-edgetunnel",
  "version": "1.0.0",
  "private": true,
  "description": "Audited single-user VLESS over WebSocket entry for Cloudflare Pages.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "audit:artifact": "node scripts/audit-artifact.mjs",
    "build:pages": "node scripts/build-pages.mjs",
    "test": "node --test",
    "verify": "npm test && npm run verify:repro && npm run build:pages && npm run audit:artifact",
    "verify:repro": "node scripts/check-reproducible.mjs"
  },
  "devDependencies": {
    "esbuild": "0.27.2"
  },
  "engines": {
    "node": ">=20"
  }
}
